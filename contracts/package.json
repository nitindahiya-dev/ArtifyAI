{
  "name": "artifyai-contracts",
  "version": "0.1.0",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.6.1",
    "ethers": "^5.8.0",
    "hardhat": "^2.26.2"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  }
}
